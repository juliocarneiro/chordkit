name: Publish to npm

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - run: npm ci

      - run: npm run build --workspace=packages/chordkit

      - run: npm test --workspace=packages/chordkit

      - name: Publish chordkit
        run: npm publish --workspace=packages/chordkit
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - run: npm run build --workspace=packages/dictionary

      - name: Publish @chordkit/dictionary
        run: npm publish --workspace=packages/dictionary --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
